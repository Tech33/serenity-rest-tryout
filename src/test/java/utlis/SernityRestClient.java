package utlis;

/*
public class SernityRestClient {

    static EnvironmentVariables environmentVariables = SerenityInfrastructure.getEnvironmentVariables();
    public static Response sendRequst(HttpMethod method, String endpoint, Map<String, Object> headers,  Map<String, Object> paramerets, RequestBody body, boolean isAuthReq)

    {
        RestAssured.baseURI= environmentVariables.getProperty("restapi.baseurl");
        Response res = SerenityRest.rest().given().when().headers()
                switch(method)
                    get:
                .get("/pet/findByStatus?status="+status);

        return res;

    }

    private Headers buildHeaders (Map <String, Object> headers)
    {
       Builder builder = new Builder();
        Headers buildHeaders = new Headers(builder);


    }
}
*/
